# Data Set Update — v301

برای اتصال به Google Sheet فایل `appsscript/Code.gs` را در Google Apps Script قرار دهید و به صورت Web App Deploy کنید. URL نهایی `/exec` را در صفحه Data Set Update وارد کنید.

Distribution Centers از همان `extract-dc` استخراج می‌شود. مقصد تب `Distribution Centers (LG)` است. قبل از نوشتن، محتوای کل تب پاک می‌شود و سپس Header در ردیف 1 و داده‌ها از ردیف 2 نوشته می‌شوند.


نسخه v302: همگام‌سازی تم با تم اصلی Digiexpress و سازگاری با Host 12.5.1 برای اجرای صحیح داخل iframe.
